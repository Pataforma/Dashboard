import type { Config } from "tailwindcss";

// This file is now empty as we've migrated to Bootstrap
// Keeping it for potential future use or gradual migration back if needed
export default {
	content: [],
	theme: {
		extend: {},
	},
	plugins: [],
} satisfies Config;
